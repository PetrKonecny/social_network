<li id="<%= message.id %>_<%= message.created_at.strftime("%d_%b_%Y_%I_%M_%S_%p") %>" class="<%=  self_or_other(message) %>">
    <div class="avatar">
        <img src="http://placehold.it/50x50" />
    </div>
    <div class="chatboxmessagecontent">
        <p><%= message.body %></p>
        <time datetime="<%= message.created_at %>" title="<%= message.created_at.strftime("%d %b  %Y at %I:%M%p") %>">
        <%= message_interlocutor(message).profile.full_name %> • <%= message.created_at.strftime("%H:%M %p") %>
        </time>
    </div>
</li>